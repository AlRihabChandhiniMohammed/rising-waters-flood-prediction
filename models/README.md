# Models

This directory stores trained machine learning models.

- Models are serialized with `joblib` (`.pkl` / `.joblib`).
- The directory is gitignored; `.gitkeep` preserves this folder in version
  control.
- Add a brief description of each model below.

## Available Models

<!-- TODO: List trained models with accuracy metrics -->
